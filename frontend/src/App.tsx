/**
 * Root component of the application.
 *
 * For now this is a placeholder used to verify the toolchain
 * (Vite + React + Tailwind) is wired up correctly. Real
 * routes and pages will replace this in Task 9.5 onward.
 */
function App() {
  return (
    <div className="min-h-screen bg-slate-50 flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-slate-900 mb-2">
          Manual File Uploader
        </h1>
        <p className="text-slate-600">
          Frontend scaffolding complete — Tailwind is working.
        </p>
      </div>
    </div>
  )
}

export default App